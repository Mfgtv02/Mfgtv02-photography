import Link from "next/link";
import styles from "./heading.module.css"; 

const heading = () => {
  return (
      <div className={styles.heading}>
        <h1 className={styles.heading}>Fotografia Z Pasji</h1>
      </div>

  );
};

export default heading;